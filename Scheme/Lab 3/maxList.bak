#lang racket

;; maxList: Takes in a list of numbers and returns the highest
;; (maxList '(6 2 1 87 9 1))

(define maxList
  (lambda (L)
    (cond ((= (length L) 0) 0)
          ( #t 1 )
          )))
